<% if hints.empty? %>
    <em>Подсказок нет</em>
<% else %>
    <% hints.each do |hint| %>
        <p>
          <em>Через</em> <strong><%= link_to hint.delay_in_minutes, resource(hint.level.game, hint.level, hint, :edit) %></strong> <em>минут:</em>
          <em>
            <%= link_to "(редактировать)", resource(hint.level.game, hint.level, hint, :edit) %>
            <%= link_to "(удалить)", resource(hint.level.game, hint.level, hint, :delete)%>
          </em>
        </p>
        <p>
          <%= hint.text %>
        </p>
        <br />
    <% end %>
<% end %>